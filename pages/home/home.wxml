<!--index.wxml-->
<view class="container">
    <navBar parameter='{{parameter}}' logoUrl="{{logoUrl}}"></navBar>
    <videoItem item='{{item}}' wx:for='{{videos}}' wx:key='{{index}}'></videoItem>
    <tabBar></tabBar>
</view>